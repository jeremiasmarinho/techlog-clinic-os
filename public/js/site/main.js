/**
 * Site Main Script
 * Landing page interactions (smooth scroll, animations, etc.)
 * This module is independent from CRM and Chat
 */

// Smooth scroll for anchor links
document.addEventListener('DOMContentLoaded', function() {
    // Add any landing page specific logic here
    console.log('Site module loaded');
});
