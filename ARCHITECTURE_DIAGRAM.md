# Frontend Architecture Diagram

## Module Dependency Graph

```
┌─────────────────────────────────────────────────────────────────┐
│                         ADMIN PANEL                             │
│                       (admin.html)                              │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Header     │  │  Team Mgmt   │  │  Dashboard   │         │
│  │   - User     │  │   Modal      │  │    Modal     │         │
│  │   - Logout   │  │  - Add User  │  │  - Charts    │         │
│  │   - Team Btn │  │  - User List │  │  - Metrics   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              KANBAN BOARD (4 Columns)                   │   │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────────┐           │   │
│  │  │ Novo │  │ Em   │  │Agen- │  │Finaliza- │           │   │
│  │  │      │  │Atend.│  │dado  │  │   do     │           │   │
│  │  └──────┘  └──────┘  └──────┘  └──────────┘           │   │
│  │   (Drag & Drop enabled)                                 │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────────────────────────┐
        │         SCRIPT LOADING ORDER             │
        │                                          │
        │  1. Chart.js (CDN)                       │
        │     └─> External charting library        │
        │                                          │
        │  2. api.js                               │
        │     ├─> API_URL, ACCESS_TOKEN            │
        │     ├─> notificationSound                │
        │     ├─> showLoading()                    │
        │     └─> showNotification()               │
        │                                          │
        │  3. auth.js (depends on api.js)          │
        │     ├─> currentUser check                │
        │     ├─> logout()                         │
        │     ├─> openTeamModal()                  │
        │     ├─> loadUsers()                      │
        │     └─> deleteUser()                     │
        │                                          │
        │  4. kanban.js (depends on api.js)        │
        │     ├─> loadLeads()                      │
        │     ├─> renderLeads()                    │
        │     ├─> createLeadCard()                 │
        │     ├─> Drag & Drop handlers             │
        │     └─> togglePrivacyMode()              │
        │                                          │
        │  5. dashboard.js (depends on api.js)     │
        │     ├─> openDashboard()                  │
        │     ├─> renderStatusChart()              │
        │     ├─> renderTypeChart()                │
        │     └─> renderHistoryChart()             │
        └─────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                       LANDING PAGE                              │
│                       (index.html)                              │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐     │
│  │                  Hero Section                         │     │
│  │  - Welcome message                                    │     │
│  │  - Benefits showcase                                  │     │
│  │  - Call-to-action                                     │     │
│  └──────────────────────────────────────────────────────┘     │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐     │
│  │            WhatsApp Chatbot Widget                    │     │
│  │   ┌─────────────────────────────────┐                │     │
│  │   │  💬 Chat Window (hidden)        │                │     │
│  │   │  ┌───────────────────────────┐  │                │     │
│  │   │  │ Step 1: Name              │  │                │     │
│  │   │  │ Step 2: Type Selection    │  │                │     │
│  │   │  │ Step 3: Phone Number      │  │                │     │
│  │   │  │ Step 4: API Submission    │  │                │     │
│  │   │  └───────────────────────────┘  │                │     │
│  │   └─────────────────────────────────┘                │     │
│  │   [💬] Floating Button                               │     │
│  └──────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
              ┌─────────────────────────┐
              │   SCRIPT LOADING        │
              │                         │
              │  1. chatbot.js          │
              │     ├─> chatState       │
              │     ├─> toggleChat()    │
              │     ├─> initializeChat()│
              │     ├─> handleNameInput │
              │     ├─> selectType()    │
              │     ├─> handlePhone     │
              │     └─> submitToAPI()   │
              └─────────────────────────┘


┌────────────────────────────────────────────────────────────────────┐
│                       DATA FLOW DIAGRAM                             │
└────────────────────────────────────────────────────────────────────┘

    USER                FRONTEND               BACKEND              DATABASE
     │                    │                      │                     │
     │─── Load Page ─────>│                      │                     │
     │                    │                      │                     │
     │                    │──── Auth Check ─────>│                     │
     │                    │<─── User Data ───────│                     │
     │                    │                      │                     │
     │                    │──── GET /leads ─────>│                     │
     │                    │                      │─── Query ─────────>│
     │                    │                      │<─── Results ────────│
     │                    │<─── Leads JSON ──────│                     │
     │                    │                      │                     │
     │<─── Render Board ──│                      │                     │
     │                    │                      │                     │
     │─ Drag Card ───────>│                      │                     │
     │                    │─ PATCH /leads/:id ──>│                     │
     │                    │                      │─ UPDATE ──────────>│
     │                    │                      │<─ Success ──────────│
     │                    │<─ 200 OK ────────────│                     │
     │                    │                      │                     │
     │<─ Notification ────│                      │                     │
     │                    │                      │                     │
     │─ Open Dashboard ──>│                      │                     │
     │                    │─ GET /dashboard ────>│                     │
     │                    │                      │─ Aggregate ───────>│
     │                    │                      │<─ Stats ────────────│
     │                    │<─ Metrics JSON ──────│                     │
     │                    │                      │                     │
     │<─ Render Charts ───│                      │                     │


┌────────────────────────────────────────────────────────────────────┐
│                    FILE ORGANIZATION                                │
└────────────────────────────────────────────────────────────────────┘

public/
├── 📄 index.html (495 lines)           - Landing page
├── 📄 admin.html (430 lines)           - Admin panel
├── 📄 login.html                       - Authentication page
├── 📄 widget.js                        - Chat widget embed code
│
└── js/
    ├── 🔧 api.js (42 lines)            - Foundation module
    │   ├─ API_URL
    │   ├─ ACCESS_TOKEN
    │   ├─ notificationSound
    │   ├─ showLoading()
    │   └─ showNotification()
    │
    ├── 🔐 auth.js (183 lines)          - Authentication module
    │   ├─ currentUser (from localStorage)
    │   ├─ logout()
    │   ├─ openTeamModal()
    │   ├─ closeTeamModal()
    │   ├─ loadUsers()
    │   ├─ addUserForm handler
    │   └─ deleteUser()
    │
    ├── 📊 kanban.js (453 lines)        - Lead management module
    │   ├─ loadLeads()
    │   ├─ renderLeads()
    │   ├─ createLeadCard()
    │   ├─ dragStart/dragEnd/allowDrop/drop()
    │   ├─ deleteLead()
    │   ├─ updateCounters()
    │   ├─ togglePrivacyMode()
    │   ├─ openEditModal()
    │   ├─ closeEditModal()
    │   └─ formatPhone(), getTimeAgo()
    │
    ├── 📈 dashboard.js (230 lines)     - Analytics module
    │   ├─ openDashboard()
    │   ├─ closeDashboard()
    │   ├─ renderStatusChart()
    │   ├─ renderTypeChart()
    │   └─ renderHistoryChart()
    │
    └── 💬 chatbot.js (340 lines)       - Chat widget module
        ├─ chatState (step-based flow)
        ├─ toggleChat(), openChat(), closeChat()
        ├─ initializeChat()
        ├─ addBotMessage(), addUserMessage()
        ├─ showTextInput(), showTypeButtons(), showPhoneInput()
        ├─ handleNameInput()
        ├─ selectType()
        ├─ handlePhoneInput()
        ├─ submitLeadToAPI()
        └─ restartChat()


┌────────────────────────────────────────────────────────────────────┐
│                    MODULE RELATIONSHIPS                             │
└────────────────────────────────────────────────────────────────────┘

              ┌──────────────────────────────┐
              │        Chart.js (CDN)        │
              │    External Dependency       │
              └──────────────────────────────┘
                          ↑
                          │ (uses)
                          │
              ┌──────────────────────────────┐
              │          api.js              │
              │    (Foundation Module)       │
              │  - API configuration         │
              │  - Utility functions         │
              └──────────────────────────────┘
                    ↑     ↑     ↑
                    │     │     │
       ┌────────────┘     │     └────────────┐
       │                  │                  │
       │                  │                  │
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   auth.js    │  │  kanban.js   │  │dashboard.js  │
│              │  │              │  │              │
│ - Auth check │  │ - Kanban     │  │ - Charts     │
│ - Team mgmt  │  │ - Drag/Drop  │  │ - Metrics    │
└──────────────┘  └──────────────┘  └──────────────┘


   ┌──────────────────────────────┐
   │       chatbot.js             │
   │    (Standalone Module)       │
   │  - Chat widget logic         │
   │  - Lead capture flow         │
   └──────────────────────────────┘


┌────────────────────────────────────────────────────────────────────┐
│                   COMMUNICATION PATTERNS                            │
└────────────────────────────────────────────────────────────────────┘

HTML onclick → Global Function → Module Logic
  │              │                  │
  Example:       │                  │
  <button        │                  │
   onclick=      │                  │
   "logout()">   └─> logout()       │
                     (in auth.js)   │
                                    │
                                    └─> localStorage.removeItem()
                                        window.location.href = '/login.html'


Auto-refresh Pattern:
  ┌────────────────────────────────────────┐
  │  setInterval(loadLeads, 60000)         │
  │     └─> Every 60 seconds               │
  │         └─> Fetch new leads from API   │
  │             └─> Update UI              │
  └────────────────────────────────────────┘


Event Delegation Pattern:
  ┌────────────────────────────────────────┐
  │  DOMContentLoaded                      │
  │     └─> Initialize form listeners      │
  │         └─> editForm.addEventListener  │
  │             └─> Handle submission      │
  └────────────────────────────────────────┘
